import { createAction } from '@lib/redux';

export const showSubscribePerformerModal = createAction('SHOW_SUBSCRIBE_PERFORMER_MODAL');
export const hideSubscribePerformerModal = createAction('HIDE_SUBSCRIBE_PERFORMER_MODAL');
